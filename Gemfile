source 'https://rubygems.org'

#deleteGemfile.lock from your project if it has it in gitignore
gem 'sinatra'
gem 'sinatra-contrib'
gem 'sequel'

gem 'twilio-ruby'
gem 'bcrypt'

#should add this into your project too and remove the sql gemfile
group :development do 
 gem 'sqlite3'
end
group :production do
  gem 'pg'
end